$kind: http-request
name: Update Order Status - Admin
url: "{{HOST}}api/orders/admin/{{order_id}}"
method: PUT
body:
  type: json
  content: |-
    {
      "status": "processing"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3
