$kind: http-request
name: Add Route
url: "{{HOST}}route"
method: POST
body:
  type: json
  content: |-
    {
      "prefix": "/api/products",
      "target": "http://product-service:3002"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2
